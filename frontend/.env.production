# Same-origin API via Vercel rewrite (see vercel.json).
# Avoids brittle api.* DNS and cross-origin cookie issues.
# If Vercel Dashboard also sets VITE_API_URL, that value wins — set it to /api/v1 there too.
VITE_API_URL=/api/v1
