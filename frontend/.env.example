# Local: leave unset to use Vite's /api proxy (vite.config.js → localhost:8000).
# Production (Vercel): use same-origin /api/v1 (proxied to Render via vercel.json).
# Do NOT point the browser at api.txnnova.* — custom API DNS is flaky and breaks login.
VITE_API_URL=/api/v1
