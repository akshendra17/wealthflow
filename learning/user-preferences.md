# User Preferences

- Product UI branding: **TxnNova** (repo may still say WealthFlow).
- Production hosting: Frontend on Vercel, Backend on Render, DB on Neon.
- Custom domain: `txnnova.byakshendra.in` (GoDaddy). Prefer same-origin API via Vercel `/api` rewrite over `api.*` subdomain for browser calls.
