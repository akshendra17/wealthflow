DATABASE_URL=postgresql+asyncpg://wealthflow:wealthflow_dev_2026@localhost:5432/wealthflow
# Comma-separated or JSON list. Must include the exact browser Origin (scheme + host, no path).
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000","https://txnnova.byakshendra.in"]
# production enables Secure cookies (required for HTTPS refresh tokens)
ENVIRONMENT=development
DEBUG=true
UPLOAD_DIR=./uploads
